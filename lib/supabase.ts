/**
 * Public (anon) Supabase client — for client-side use only.
 * All server-side database access uses lib/supabase-server.ts (service_role key).
 *
 * Currently unused: the app has no direct client→Supabase calls.
 * Kept here for future client-side features (e.g. real-time subscriptions).
 */
